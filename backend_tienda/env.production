# Variables de entorno para despliegue en AWS EC2 (Ubuntu 22.04)
# Copia este archivo a ".env" en el servidor y reemplaza los valores antes de ejecutar deploy.sh

APP_ENV=production
PYTHONPATH=./app
DEBUG=False

# Amazon Aurora PostgreSQL
# Formato: postgresql://usuario:password@host:puerto/base
DATABASE_URL=postgresql://postgres:Ecommerce2025!@ecommerce-aurora-db.c5qc4qo884xb.us-east-2.rds.amazonaws.com:5432/postgres

# Seguridad
SECRET_KEY=cambia_esto_por_un_valor_ultra_secreto
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS: lista separada por comas de los orígenes permitidos (sin espacios)
CORS_ORIGINS=https://api.tienda.com,https://admin.tienda.com

# Configuración recomendada para pools/conexiones (optimizado para Aurora)
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=5
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# Gunicorn / logs
GUNICORN_BIND=unix:/run/fastapi-ecommerce.sock
GUNICORN_WORKERS=3
GUNICORN_TIMEOUT=120
GUNICORN_LOG_LEVEL=info
LOG_LEVEL=INFO

# Metadata útil
PROJECT_NAME=fastapi-ecommerce
AWS_REGION=us-east-2
ENVIRONMENT=production

